<Window x:Class="OffsetRecovery.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameHelper2 Offset Recovery" Height="660" Width="1040"
        WindowStartupLocation="CenterScreen">
    <DockPanel Margin="10">

        <!-- File picker / run -->
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Game .exe:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Grid.Column="1" x:Name="PathBox" VerticalContentAlignment="Center" Height="26"/>
            <Button Grid.Column="2" x:Name="BrowseButton" Content="Browse…" Margin="8,0,0,0" Padding="12,4" Click="BrowseClick"/>
            <Button Grid.Column="3" x:Name="RecoverButton" Content="Recover" Margin="8,0,0,0" Padding="18,4" Click="RecoverClick"/>
        </Grid>

        <TextBlock DockPanel.Dock="Top" x:Name="StatsText" Margin="0,8,0,4" Foreground="#555"
                   Text="Choose a game executable and press Recover."/>
        <ProgressBar DockPanel.Dock="Top" x:Name="Progress" Height="4" IsIndeterminate="False" Margin="0,0,0,8"/>

        <!-- Log -->
        <GroupBox DockPanel.Dock="Bottom" Header="Log" Height="180" Margin="0,8,0,0">
            <TextBox x:Name="LogBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"
                     TextWrapping="NoWrap" Background="#1E1E1E" Foreground="#DDDDDD"/>
        </GroupBox>

        <!-- Actions -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
            <Button x:Name="CopyPatternButton" Content="Copy pattern" Padding="12,4" Click="CopyPatternClick"/>
            <Button x:Name="CopyAddressButton" Content="Copy address" Padding="12,4" Margin="8,0,0,0" Click="CopyAddressClick"/>
            <Button x:Name="SaveJsonButton" Content="Save offsets.json…" Padding="12,4" Margin="8,0,0,0" Click="SaveJsonClick"/>
            <CheckBox x:Name="VerboseCheck" Content="Verbose log" VerticalAlignment="Center" Margin="16,0,0,0"/>
        </StackPanel>

        <!-- Results -->
        <DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontFamily="Consolas" FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Offset" Binding="{Binding Name}" Width="170"/>
                <DataGridTextColumn Header="Resolved" Binding="{Binding ResolvedAddress}" Width="130"/>
                <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence}" Width="110"/>
                <DataGridTextColumn Header="Matches" Binding="{Binding Matches}" Width="70"/>
                <DataGridTextColumn Header="Skip" Binding="{Binding BytesToSkip}" Width="50"/>
                <DataGridTextColumn Header="Pattern" Binding="{Binding Pattern}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</Window>
